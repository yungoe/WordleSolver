#This function takes a file name and reads it line by line into an array, then sorts the array based on the frequency of the letters in each word.
def MakeSortedWordList(filename):
#Load the word list into an array of arrays (one letter per cell)
    wordlist = []
#Count all the letters in the word list
#For each letter in the alphabet, count the number of times it occurs in the word list
#For Each letter of the alphabet, determine the ratio of the total and multiply by 1000.  This is the letter score
#determine a score for each word as the sum of the letter scores.  Add the score (interger) as the first element of the array
#sort the array by the scores
#return the array
    return wordlist